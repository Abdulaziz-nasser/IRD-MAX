# Starter ranges from one_camera_robot_rebuild_source.zip/config/vision.yaml.
# Flattened for the existing programs; NOT field-calibrated values.
# Use the sampler to create a measured vision_*.yaml file.
BLUE:
  - low: [90, 80, 35]
    high: [135, 255, 255]
ORANGE:
  - low: [5, 90, 45]
    high: [28, 255, 255]
RED:
  - low: [0, 90, 35]
    high: [12, 255, 255]
  - low: [168, 90, 35]
    high: [179, 255, 255]
GREEN:
  - low: [35, 65, 30]
    high: [90, 255, 255]

1